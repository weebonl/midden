services:
  midden:
    environment:
      MIDDEN__DATABASE__URL: "sqlite:///var/lib/midden/midden.db?mode=rwc"
      MIDDEN__STORAGE__BACKEND: "local"
      MIDDEN__STORAGE__LOCAL__PATH: "/var/lib/midden/blobs"
    volumes:
      - midden-data:/var/lib/midden

volumes:
  midden-data:
